## 1. AsyncAPI Telemetry Setup

- [ ] 1.1 Create the directory structure `docs/architecture/api_contracts/`
- [ ] 1.2 Create an initial, skeleton `asyncapi.yaml` defining the basic Kafka/NATS event channels the agent subscribes to

## 2. Executable Specifications (Behave)

- [ ] 2.1 Add `behave` to the project's development dependencies (e.g., in `pyproject.toml`)
- [ ] 2.2 Create the BDD folder structure: `tests/features/` and `tests/features/steps/`
- [ ] 2.3 Create a `tests/features/README.md` explaining how to translate OpenSpec scenarios into `.feature` files
- [ ] 2.4 Create a sample `.feature` file for one of the existing OpenSpec capabilities to prove the integration works
