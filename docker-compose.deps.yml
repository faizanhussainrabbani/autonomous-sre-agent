version: "3.8"

# ===========================================================================
# SRE Agent — Development Dependencies
#
# Usage:
#   docker compose -f docker-compose.deps.yml up -d      # start all
#   docker compose -f docker-compose.deps.yml down        # stop all
#
# Or use the helper script:
#   /Users/faizanhussain/Documents/Project/Practice/AiOps/scripts/dev/setup_deps.sh start
#   /Users/faizanhussain/Documents/Project/Practice/AiOps/scripts/dev/setup_deps.sh stop
# ===========================================================================

services:

  # ── LocalStack Pro ────────────────────────────────────────────────────────
  # Emulates AWS APIs locally (ECS, EC2, Lambda, IAM, S3, SecretsManager).
  # Required for integration tests, demos, and AWS workflow validation.
  # Standardized usage is documented in docs/testing/localstack_pro_usage_standard.md.
  localstack:
    image: localstack/localstack-pro:latest
    container_name: localstack
    ports:
      - "4566:4566"
    environment:
      - LOCALSTACK_AUTH_TOKEN=${LOCALSTACK_AUTH_TOKEN:?LOCALSTACK_AUTH_TOKEN is required for LocalStack Pro}
      - DEBUG=0
      - SERVICES=autoscaling,cloudwatch,ec2,ecs,events,iam,lambda,logs,s3,secretsmanager,sns,sts
      - DEFAULT_REGION=us-east-1
      - LAMBDA_RUNTIME_ENVIRONMENT_TIMEOUT=120
      - EAGER_SERVICE_LOADING=1
      - DOCKER_HOST=unix:///var/run/docker.sock
    volumes:
      - localstack_data:/var/lib/localstack
      - /var/run/docker.sock:/var/run/docker.sock
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:4566/_localstack/health"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ── Prometheus ────────────────────────────────────────────────────────────
  # Metrics collection and querying.
  # Required for: OTel adapter tests, detection pipeline tests.
  prometheus:
    image: prom/prometheus:v2.51.0
    container_name: sre-prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./infra/prometheus:/etc/prometheus:ro
    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
      - "--storage.tsdb.retention.time=1d"
      - "--web.enable-lifecycle"
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:9090/-/healthy"]
      interval: 10s
      timeout: 5s
      retries: 3

  # ── Jaeger ────────────────────────────────────────────────────────────────
  # Distributed trace backend.
  # Required for: trace ingestion tests, OTel adapter tests.
  jaeger:
    image: jaegertracing/all-in-one:1.55
    container_name: sre-jaeger
    ports:
      - "16686:16686"   # UI
      - "14268:14268"   # Collector HTTP
      - "4317:4317"     # OTLP gRPC
      - "4318:4318"     # OTLP HTTP
    environment:
      - COLLECTOR_OTLP_ENABLED=true

volumes:
  localstack_data:
